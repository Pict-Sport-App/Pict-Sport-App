import 'package:flutter/material.dart';

class BadmintonPlaying extends StatelessWidget {
  const BadmintonPlaying({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
