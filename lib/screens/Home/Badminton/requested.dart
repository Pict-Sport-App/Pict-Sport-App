import 'package:flutter/material.dart';

class BadmintonRequested extends StatelessWidget {
  const BadmintonRequested({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
