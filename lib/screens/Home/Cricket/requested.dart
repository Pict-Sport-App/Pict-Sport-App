import 'package:flutter/material.dart';

class RequestedCricket extends StatelessWidget {
  const RequestedCricket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
