import 'package:flutter/material.dart';

class ReturnedCricket extends StatelessWidget {
  const ReturnedCricket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
